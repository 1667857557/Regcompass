diff --git a/tests/testthat/test-audit-contract-hardening.R b/tests/testthat/test-audit-contract-hardening.R
index ed8ea0e..b14d26a 100644
--- a/tests/testthat/test-audit-contract-hardening.R
+++ b/tests/testthat/test-audit-contract-hardening.R
@@ -27,8 +27,14 @@ test_that("workflow OR aggregation matches the recorded parameter", {
   expect_match(worker_text, "capacity_params = capacity_params")
 
   workflow_text <- paste(deparse(body(rc_run_regcompass)), collapse = "\n")
-  expect_match(workflow_text, "gpr_or_method = or_method")
-  expect_match(workflow_text, "gpr_or_method_source = or_method_source")
+  expect_match(
+    workflow_text,
+    "answer\\$params\\$gpr_or_method <- or_method"
+  )
+  expect_match(
+    workflow_text,
+    "answer\\$params\\$gpr_or_method_source <- or_method_source"
+  )
 })
 
 
diff --git a/tests/testthat/test-human-medium-presets.R b/tests/testthat/test-human-medium-presets.R
index d6b53f8..a856378 100644
--- a/tests/testthat/test-human-medium-presets.R
+++ b/tests/testthat/test-human-medium-presets.R
@@ -138,7 +138,10 @@ test_that("RPMI-1640 enables basal formulation nutrients but not plasma-only row
   ) %in% medium$exchange_reaction_id))
   expect_false("EX_lactate" %in% medium$exchange_reaction_id)
   expect_false("EX_urate" %in% medium$exchange_reaction_id)
-  expect_equal(medium_row(medium, "EX_glucose")$concentration_mM, 11.1)
+  expect_equal(
+    medium_row(medium, "EX_glucose")$concentration_mM,
+    11.111111, tolerance = 1e-6
+  )
   expect_match(
     medium_row(medium, "EX_glucose")$reference_label,
     "RPMI|Moore"
diff --git a/tests/testthat/test-species-medium-refactor.R b/tests/testthat/test-species-medium-refactor.R
index e254b05..8e8af1a 100644
--- a/tests/testthat/test-species-medium-refactor.R
+++ b/tests/testthat/test-species-medium-refactor.R
@@ -84,7 +84,7 @@ test_that("Mouse-GEM GPR rules retain mouse symbols directly", {
   keys <- sort(vapply(groups, function(x) paste(sort(x), collapse = "+"),
                       character(1)))
 
-  expect_equal(keys, c("Adh1", "Adh4+Aldh2"))
+  expect_equal(unname(keys), c("Adh1", "Adh4+Aldh2"))
   expect_false(any(grepl("ENSG", prepared$gpr_table$gene)))
 })
 
diff --git a/tests/testthat/test_boltzmann.R b/tests/testthat/test_boltzmann.R
index fc12d26..0989521 100644
--- a/tests/testthat/test_boltzmann.R
+++ b/tests/testthat/test_boltzmann.R
@@ -11,7 +11,8 @@ test_that("rc_reaction_capacity_one handles AND and OR semantics", {
   and_part <- rc_boltzmann_minavg(c(0.2, 0.8), tau = 0.08)
   expect_equal(
     rc_reaction_capacity_one(
-      parsed, gene_score, tau = 0.08, or_method = "sum_sqrtK"
+      parsed, gene_score, tau = 0.08, and_method = "boltzmann",
+      or_method = "sum_sqrtK"
     ),
     (and_part + 0.5) / sqrt(2)
   )
