diff --git a/R/calibration_q95.R b/R/calibration_q95.R
index 3e89773..0d77f90 100644
--- a/R/calibration_q95.R
+++ b/R/calibration_q95.R
@@ -162,6 +162,22 @@ rc_q95_shrink <- function(
   Q <- do.call(rbind, diagnostics)
   Q$all_missing_reaction_flag <- Q$n_global == 0L
   Q$stratum_missing_reaction_flag <- Q$n == 0L
+  # Compatibility aliases retained for downstream diagnostics and older tests.
+  Q$q_shrink <- Q$q_value
+  Q$q95_power_class <- factor(
+    ifelse(
+      Q$n < 5L,
+      "very_low",
+      ifelse(
+        Q$n < 20L,
+        "low",
+        ifelse(Q$n < 100L, "moderate",
+               ifelse(Q$n < 400L, "adequate", "high"))
+      )
+    ),
+    levels = c("very_low", "low", "moderate", "adequate", "high"),
+    ordered = TRUE
+  )
   list(C_rel = relative, Q = Q, weights = weights)
 }
 
diff --git a/R/global_workflow.R b/R/global_workflow.R
index c373653..149ae77 100644
--- a/R/global_workflow.R
+++ b/R/global_workflow.R
@@ -168,6 +168,13 @@
     C_raw, weights, eps = 1e-6, n0 = 80,
     unit_meta = NULL, stratum_col = NULL,
     bootstrap = FALSE, B = 500, BPPARAM = NULL) {
+  C_raw <- as.matrix(C_raw)
+  if (is.null(rownames(C_raw))) {
+    rownames(C_raw) <- paste0("reaction_", seq_len(nrow(C_raw)))
+  }
+  if (is.null(colnames(C_raw))) {
+    colnames(C_raw) <- paste0("unit_", seq_len(ncol(C_raw)))
+  }
   rc_q95_calibrate(
     C_raw = C_raw,
     eps = eps,
diff --git a/R/gpr_parser.R b/R/gpr_parser.R
index 1935370..01cdf92 100644
--- a/R/gpr_parser.R
+++ b/R/gpr_parser.R
@@ -6,6 +6,9 @@
 #'
 #' @param gpr One GPR expression.
 #' @param max_terms Maximum number of DNF alternatives allowed during expansion.
+#' @param preserve_case Retain source gene-symbol capitalization. The default
+#'   lower-cases computational identifiers; Mouse-GEM import opts into native
+#'   mouse symbol capitalization.
 #' @return A list of character vectors.
 .rc_gpr_tokenize <- function(gpr) {
   text <- gsub("([()])", " \\1 ", trimws(as.character(gpr)))
@@ -45,7 +48,8 @@
   answer[!duplicated(keys)]
 }
 
-rc_parse_gpr_simple <- function(gpr, max_terms = 10000L) {
+rc_parse_gpr_simple <- function(gpr, max_terms = 10000L,
+                                preserve_case = FALSE) {
   if (length(gpr) != 1L || is.na(gpr) || !nzchar(trimws(gpr))) return(list())
   if (!is.numeric(max_terms) || length(max_terms) != 1L ||
       !is.finite(max_terms) || max_terms < 1) {
@@ -114,7 +118,8 @@ rc_parse_gpr_simple <- function(gpr, max_terms = 10000L) {
     )
   }
   lapply(.rc_gpr_ast_to_dnf(ast, as.integer(max_terms)), function(x) {
-    unique(x[nzchar(x)])
+    x <- unique(x[nzchar(x)])
+    if (isTRUE(preserve_case)) x else tolower(x)
   })
 }
 
diff --git a/R/humangem.R b/R/humangem.R
index d7c2a22..cff496f 100644
--- a/R/humangem.R
+++ b/R/humangem.R
@@ -477,7 +477,9 @@ rc_prepare_species_gpr_table <- function(
       rule <- rc_replace_gem_gene_ids(rule, gene_map)
     }
     parsed <- tryCatch(
-      rc_parse_gpr_simple(rule),
+      rc_parse_gpr_simple(
+        rule, preserve_case = identical(species, "mouse")
+      ),
       error = function(error) {
         stop(
           "Failed to parse ", spec$repository_name, " GPR for reaction `",
@@ -661,7 +663,9 @@ rc_convert_yaml_to_regcompass <- function(
     )
     if (nzchar(rule)) {
       parsed <- tryCatch(
-        rc_parse_gpr_simple(rule),
+        rc_parse_gpr_simple(
+          rule, preserve_case = identical(species, "mouse")
+        ),
         error = function(error) {
           stop(
             "Failed to parse ", spec$repository_name,
diff --git a/R/microcompass.R b/R/microcompass.R
index f98db48..c036f36 100644
--- a/R/microcompass.R
+++ b/R/microcompass.R
@@ -433,11 +433,29 @@ rc_run_microcompass <- function(
     )
   }
   if (is.null(medium_scenarios) && is.null(medium_table)) {
-    medium_scenarios <- rc_make_medium_scenarios(
-      gem,
-      scenario = "physiologic",
-      species = "auto"
-    )
+    model_info <- gem$model_info %||% list()
+    recorded_species <- tolower(as.character(model_info$species %||% ""))
+    recorded_source <- tolower(as.character(model_info$source %||% ""))
+    species_provenance <- recorded_species %in% c("human", "mouse") ||
+      grepl("human-gem|mouse-gem", recorded_source)
+    if (isTRUE(species_provenance)) {
+      medium_scenarios <- rc_make_medium_scenarios(
+        gem,
+        scenario = "physiologic",
+        species = "auto",
+        strict_preset_matching = TRUE
+      )
+    } else {
+      warning(
+        "GEM species provenance is unavailable; preserving the model's original exchange directions instead of assuming a human physiological medium.",
+        call. = FALSE
+      )
+      medium_scenarios <- rc_make_medium_scenarios(
+        gem,
+        scenario = "compass_model_bounds",
+        species = "auto"
+      )
+    }
   }
   cache_dir <- model_params$cache_dir %||% NULL
   if (!is.null(cache_dir)) {
diff --git a/tests/testthat/test_balanced_local_fastcore.R b/tests/testthat/test_balanced_local_fastcore.R
index c31a65a..f374017 100644
--- a/tests/testthat/test_balanced_local_fastcore.R
+++ b/tests/testthat/test_balanced_local_fastcore.R
@@ -59,6 +59,7 @@ test_that("weighted gene score and Q95 retain matrix dimensions and finite bound
   expect_true(all(score >= 0 & score <= 1))
 
   capacity <- rbind(R1 = c(0.1, 0.2, 1.0), R2 = c(1, 1, 1))
+  colnames(capacity) <- colnames(expression)
   calibrated <- .rc_weighted_q95_calibrate(capacity, weights)
   expect_identical(dim(calibrated$C_rel), dim(capacity))
   expect_true(all(calibrated$C_rel <= 1, na.rm = TRUE))
diff --git a/tests/testthat/test_boltzmann.R b/tests/testthat/test_boltzmann.R
index 8918800..fc12d26 100644
--- a/tests/testthat/test_boltzmann.R
+++ b/tests/testthat/test_boltzmann.R
@@ -9,7 +9,16 @@ test_that("rc_reaction_capacity_one handles AND and OR semantics", {
   parsed <- list(c("g1", "g2"), "g3")
   gene_score <- c(g1 = 0.2, g2 = 0.8, g3 = 0.5)
   and_part <- rc_boltzmann_minavg(c(0.2, 0.8), tau = 0.08)
-  expect_equal(rc_reaction_capacity_one(parsed, gene_score, tau = 0.08), (and_part + 0.5) / sqrt(2))
+  expect_equal(
+    rc_reaction_capacity_one(
+      parsed, gene_score, tau = 0.08, or_method = "sum_sqrtK"
+    ),
+    (and_part + 0.5) / sqrt(2)
+  )
+  expect_equal(
+    rc_reaction_capacity_one(parsed, gene_score, tau = 0.08),
+    max(and_part, 0.5)
+  )
 })
 
 test_that("AND aggregation supports min, Boltzmann, and mean sensitivities", {
@@ -19,5 +28,9 @@ test_that("AND aggregation supports min, Boltzmann, and mean sensitivities", {
   boltz <- rc_and_capacity(scores, method = "boltzmann", tau = 0.20)
   expect_gte(boltz, min(scores))
   expect_lte(boltz, mean(scores))
-  expect_equal(rc_or_capacity(c(0.2, 0.5, NA)), 0.7 / sqrt(2))
+  expect_equal(
+    rc_or_capacity(c(0.2, 0.5, NA), method = "sum_sqrtK"),
+    0.7 / sqrt(2)
+  )
+  expect_equal(rc_or_capacity(c(0.2, 0.5, NA)), 0.5)
 })
diff --git a/tests/testthat/test_public_api.R b/tests/testthat/test_public_api.R
index 715fc96..243d419 100644
--- a/tests/testthat/test_public_api.R
+++ b/tests/testthat/test_public_api.R
@@ -41,8 +41,10 @@ test_that("staged override architecture has been removed", {
     pattern <- paste0("(?m)^", gsub("[.]", "\\\\.", name),
                       "[[:space:]]*<-[[:space:]]*function[[:space:]]*\\(")
     expect_equal(
-      sum(vapply(source_text, function(x) length(gregexpr(pattern, x, perl = TRUE)[[1L]]) > 0L,
-                 logical(1))),
+      sum(vapply(source_text, function(x) {
+        matches <- gregexpr(pattern, x, perl = TRUE)[[1L]]
+        any(matches > 0L)
+      }, logical(1))),
       1L,
       info = paste("canonical function", name, "must have one source definition")
     )
