diff --git a/R/layer2_penalty_lp.R b/R/layer2_penalty_lp.R
index b470fb1..13bbd08 100644
--- a/R/layer2_penalty_lp.R
+++ b/R/layer2_penalty_lp.R
@@ -105,11 +105,16 @@ rc_layer2_unit_matrices <- function(layer1, unit, sample_col, celltype_col, cond
   pm <- pm[pm$pool_id %in% colnames(C), , drop=FALSE]
   group_cols <- c(sample_col, if (condition_col %in% colnames(pm)) condition_col else NULL, celltype_col)
   gid <- interaction(pm[, group_cols, drop=FALSE], sep="|", drop=TRUE)
-  agg <- function(M) do.call(cbind, lapply(split(pm$pool_id, gid), function(cols) rowMedians_safe(M[, cols, drop=FALSE])))
+  agg <- function(M) {
+    do.call(cbind, lapply(
+      split(pm$pool_id, gid),
+      function(cols) matrixStats::rowMedians(M[, cols, drop = FALSE], na.rm = TRUE)
+    ))
+  }
   outC <- agg(C); outF <- agg(Conf)
   unit_meta <- unique(data.frame(unit_id = as.character(gid), pm[, group_cols, drop=FALSE], stringsAsFactors=FALSE))
   unit_meta <- unit_meta[match(colnames(outC), unit_meta$unit_id), , drop = FALSE]
-  rownames(unit_meta) <- NULL; list(C_rel = outC, reaction_confidence = outF, unit_meta = unit_meta, summary = "Layer1 aggregated to sample × celltype medians")
+  rownames(unit_meta) <- NULL; list(C_rel = outC, reaction_confidence = outF, unit_meta = unit_meta, summary = "Layer1 aggregated to sample x celltype medians")
 }
 
 rc_align_layer2_evidence <- function(M, rxns, fill = NA_real_) {
diff --git a/R/metacell.R b/R/metacell.R
index ba9c40f..ed44f76 100644
--- a/R/metacell.R
+++ b/R/metacell.R
@@ -310,7 +310,7 @@ rc_aggregate_fragments_by_membership <- function(fragment_files, membership, out
   path <- NULL
   if (is.list(fragment) && !is.null(fragment$path)) path <- fragment$path
   if (is.null(path)) path <- attr(fragment, "path", exact = TRUE)
-  if (is.null(path) && methods::is(fragment, "Fragment") && methods::isS4(fragment) && "path" %in% methods::slotNames(fragment)) {
+  if (is.null(path) && methods::is(fragment, "Fragment") && isS4(fragment) && "path" %in% methods::slotNames(fragment)) {
     path <- methods::slot(fragment, "path")
   }
   path <- as.character(path %||% character(0))
diff --git a/R/stats.R b/R/stats.R
index 203fb68..6aa33ef 100644
--- a/R/stats.R
+++ b/R/stats.R
@@ -221,7 +221,7 @@ rc_describe_microcompass_by_group <- function(
     output$medium_scenario, output$contrast,
     drop = TRUE, lex.order = TRUE
   )
-  output$FDR <- ave(output$p_value, adjustment_group,
+  output$FDR <- stats::ave(output$p_value, adjustment_group,
                     FUN = function(p) stats::p.adjust(p, method = p_adjust_method))
   rownames(output) <- NULL
   output
diff --git a/tests/testthat/test-vignette-contract.R b/tests/testthat/test-vignette-contract.R
index 6f04535..ad956ec 100644
--- a/tests/testthat/test-vignette-contract.R
+++ b/tests/testthat/test-vignette-contract.R
@@ -1,7 +1,14 @@
 test_that("workflow vignette follows the supported public API", {
-  root <- if (dir.exists("vignettes")) "." else file.path("..", "..")
-  vignette_file <- file.path(root, "vignettes", "regcompass-workflow.Rmd")
-  expect_true(file.exists(vignette_file))
+  candidates <- c(
+    file.path("vignettes", "regcompass-workflow.Rmd"),
+    file.path("..", "vignettes", "regcompass-workflow.Rmd"),
+    file.path("..", "..", "vignettes", "regcompass-workflow.Rmd")
+  )
+  candidates <- candidates[file.exists(candidates)]
+  if (!length(candidates)) {
+    skip("Source vignette is unavailable in the installed-package test context.")
+  }
+  vignette_file <- candidates[[1L]]
 
   text <- paste(readLines(vignette_file, warn = FALSE), collapse = "\n")
   supported <- c(
diff --git a/tests/testthat/test_public_api.R b/tests/testthat/test_public_api.R
index 243d419..97d1e7a 100644
--- a/tests/testthat/test_public_api.R
+++ b/tests/testthat/test_public_api.R
@@ -14,17 +14,21 @@ test_that("public API contains the canonical species-aware workflow", {
 
 
 test_that("staged override architecture has been removed", {
-  source_dir <- if (dir.exists("R")) {
-    "R"
-  } else {
-    normalizePath(file.path("..", "..", "R"), mustWork = TRUE)
+  description <- utils::packageDescription("RegCompassR")
+  expect_false(grepl(
+    "workflow_stage_", description$Collate %||% "", fixed = TRUE
+  ))
+
+  candidates <- c("R", file.path("..", "R"), file.path("..", "..", "R"))
+  candidates <- candidates[dir.exists(candidates)]
+  if (!length(candidates)) {
+    skip("Source R directory is unavailable in the installed-package test context.")
   }
+  source_dir <- normalizePath(candidates[[1L]], mustWork = TRUE)
   expect_length(
     list.files(source_dir, pattern = "^workflow_stage_", full.names = TRUE),
     0L
   )
-  description <- read.dcf(file.path(source_dir, "..", "DESCRIPTION"))
-  expect_false(grepl("workflow_stage_", description[1L, "Collate"], fixed = TRUE))
 
   canonical <- c(
     "rc_prepare_gem", "rc_prepare_human2_gem", "rc_prepare_mouse_gem",
diff --git a/vignettes/regcompass-workflow.Rmd b/vignettes/regcompass-workflow.Rmd
index 0a5eaea..786c47f 100644
--- a/vignettes/regcompass-workflow.Rmd
+++ b/vignettes/regcompass-workflow.Rmd
@@ -8,7 +8,7 @@ vignette: >
   %\VignetteEncoding{UTF-8}
 ---
 
-```{r setup, include=FALSE}
+```{r setup, include=FALSE, purl=FALSE}
 knitr::opts_chunk$set(
   collapse = TRUE,
   comment = "#>",
@@ -50,7 +50,7 @@ Cell barcodes must agree across the object and fragment files. Fragment files
 must be bgzip-compressed and tabix-indexed. They may already be registered on
 the chromatin assay or be supplied explicitly.
 
-```{r input-contract}
+```{r input-contract, purl=FALSE}
 required_metadata <- c("sample_id", "condition", "cell_type")
 stopifnot(inherits(object, "Seurat"))
 stopifnot(all(required_metadata %in% colnames(object[[]])))
@@ -82,7 +82,7 @@ run rather than silently discard a sample.
 Pin a Human-GEM release for reproducibility. The helper downloads, converts,
 annotates, validates, and caches the model.
 
-```{r gem}
+```{r gem, purl=FALSE}
 library(RegCompassR)
 
 gem <- rc_prepare_human2_gem(
@@ -109,7 +109,7 @@ Current optional named backgrounds are `normal_human_plasma`, `rpmi1640`,
 `low_glucose`, and `high_lactate`. Retired identifiers are intentionally not
 accepted or remapped.
 
-```{r medium}
+```{r medium, purl=FALSE}
 medium <- rc_make_medium_scenarios(
   gem = gem,
   scenario = "compass_model_bounds",
@@ -133,7 +133,7 @@ The explicit entry point makes every major input visible. The PFM collection
 and genome object shown below are typical human defaults; use objects matching
 the organism and genome build of the ATAC assay.
 
-```{r explicit-run}
+```{r explicit-run, purl=FALSE}
 library(Pando)
 library(BSgenome.Hsapiens.UCSC.hg38)
 data(motifs, package = "Pando")
@@ -193,7 +193,7 @@ For a first run, omit `gem` and `medium_scenarios`; the one-shot function
 constructs them and delegates to the same integrated workflow. Arguments in
 `...` are passed to `rc_run_regcompass()`.
 
-```{r one-shot}
+```{r one-shot, purl=FALSE}
 result <- rc_run_regcompass_one_shot(
   object = object,
   outdir = "RegCompass_result",
@@ -218,7 +218,7 @@ takes precedence over `medium_scenario`.
 Do not begin differential analysis until the upstream barrier and sample
 coverage have been inspected.
 
-```{r inspect-result}
+```{r inspect-result, purl=FALSE}
 stopifnot(isTRUE(result$upstream_barrier$passed))
 table(result$upstream_status$status, useNA = "ifany")
 
