% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{rc_default_bpparam}
\alias{rc_default_bpparam}
\title{Build the default RegCompass parallel backend}
\usage{
rc_default_bpparam(
  workers = NULL,
  backend = c("auto", "serial", "snow", "multicore")
)
}
\arguments{
\item{workers}{Optional worker count. Defaults to option/env/autodetection.}

\item{backend}{Parallel backend. \code{auto} avoids forked multicore workers in
detected containers; \code{serial} always returns \code{NULL}.}
}
\value{
A \code{BiocParallelParam} object when BiocParallel is installed and more
than one worker is requested; otherwise \code{NULL} for sequential execution.
}
\description{
Expensive pool-, reaction-, and bootstrap-level loops use this helper when
callers do not provide an explicit \code{BiocParallelParam}. By default it uses a
conservative worker count from \code{rc_available_workers()}. Set
\code{options(RegCompassR.workers = 1)} or \code{REGCOMPASS_WORKERS=1} to force
sequential execution.
}
