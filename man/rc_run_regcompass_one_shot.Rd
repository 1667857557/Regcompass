% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/one_shot.R
\name{rc_run_regcompass_one_shot}
\alias{rc_run_regcompass_one_shot}
\title{Run RegCompass from species-aware defaults}
\usage{
rc_run_regcompass_one_shot(object, outdir, genome,
  species = c("human", "mouse"), gem = NULL, gem_version = NULL,
  gem_source = c("auto", "bundled", "download"), pfm = NULL,
  fragment_files = FALSE, medium_scenario = NULL,
  medium_scenarios = NULL,
  progress = getOption("RegCompassR.progress", TRUE), ...)
}
\arguments{
\item{object}{Paired single-cell RNA+ATAC Seurat object.}
\item{outdir}{Persistent workflow output directory.}
\item{genome}{Genome object matching ATAC coordinates and regulatory regions.}
\item{species}{\code{"human"} or \code{"mouse"}.}
\item{gem}{Optional prepared GEM. The pinned species model is loaded when omitted.}
\item{gem_version}{Pinned model release.}
\item{gem_source}{Automatic, bundled-only, or download model source.}
\item{pfm}{Optional motif collection. Pando's bundled motifs are used when omitted.}
\item{fragment_files}{Use \code{FALSE} for the existing ATAC peak-count path.}
\item{medium_scenario}{Optional publication-bound built-in scenario. The only current value is \code{"cantor2017_hplm"} for Human-GEM.}
\item{medium_scenarios}{Optional prebuilt DOI-cited medium table. Mouse runs must supply this argument.}
\item{progress}{Display workflow and stage progress.}
\item{...}{Arguments passed to \code{rc_run_regcompass()}, including optional \code{condition_col}.}
}
\value{
A complete RegCompass result with explicit \code{analysis_mode} and stage provenance.
}
\description{
Loads species-aware defaults and runs the complete RegCompass workflow.
}
\details{
When both medium arguments are omitted, Human-GEM uses the publication-bound
\code{"cantor2017_hplm"} scenario. Mouse-GEM has no partial built-in medium and
requires an explicit DOI-cited \code{medium_scenarios} table.

The wrapped workflow automatically selects condition-aware Pando for two or more
condition levels and original \code{Pando::infer_grn()} for one or zero usable
levels. The standard route calculates no condition coefficients.

Metacells use
\code{SuperCell::SCimplify_by_graph_group_from_embedding()} with broad cell type
as \code{cell.graph.group} and condition as \code{cell.split.condition}. Each
cell type has an independent graph, all conditions within that cell type are
jointly embedded, and condition is applied only after graph clustering. Both
routes continue through the same RNA-support, GPR, reaction penalty, shared
medium-specific model, and directional LP stages. \code{regulatory_alpha = 1}
is fixed.
}