% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/one_shot.R and R/sample_bootstrap_timing_policy.R
\name{rc_run_regcompass_one_shot}
\alias{rc_run_regcompass_one_shot}
\title{Run RegCompass from species-aware defaults}
\usage{
rc_run_regcompass_one_shot(object, outdir, genome,
  species = c("human", "mouse"), gem = NULL, gem_version = NULL,
  gem_source = c("auto", "bundled", "download"), pfm = NULL,
  sample_col = NULL, fragment_files = FALSE,
  medium_scenario = "physiologic", medium_scenarios = NULL,
  progress = getOption("RegCompassR.progress", TRUE), ...)
}
\arguments{
\item{object}{Paired single-cell RNA+ATAC Seurat object.}
\item{outdir}{Persistent workflow output directory.}
\item{genome}{Genome object matching ATAC coordinates.}
\item{species}{\code{"human"} or \code{"mouse"}.}
\item{gem}{Optional prepared GEM.}
\item{gem_version}{Pinned model release.}
\item{gem_source}{Automatic, bundled-only, or explicit download model source.}
\item{pfm}{Optional motif collection.}
\item{sample_col}{Optional Stage 1 donor/sample metadata column. A valid column activates condition-stratified sample-cluster bootstrap. Omitted or absent columns print a fallback warning and use within-condition cell resampling.}
\item{fragment_files}{Use \code{FALSE} for the canonical peak-count path.}
\item{medium_scenario}{Medium preset used when \code{medium_scenarios} is omitted.}
\item{medium_scenarios}{Optional prebuilt medium table.}
\item{progress}{Display workflow and stage progress.}
\item{...}{Arguments passed to \code{rc_run_regcompass()}.}
}
\description{Loads a pinned species GEM when needed and delegates to \code{rc_run_regcompass()}.}
\details{
The sample column affects only Stage 1 bootstrap resampling. Stage 2 remains condition-only and the final model remains one shared union GEM per medium. Every workflow stage prints elapsed time in R; timing is not stored in the returned result or timing TSV files.
}
