% Generated by roxygen2: do not edit by hand
\name{rc_regcompass_step_grn}
\alias{rc_regcompass_step_grn}
\alias{rc_regcompass_step_metacells}
\alias{rc_regcompass_step_meta_modules}
\alias{rc_regcompass_step_layer1}
\alias{rc_regcompass_step_layer2}
\alias{rc_regcompass_step_results}
\title{Run the restartable RegCompass stages}
\usage{
rc_regcompass_step_grn(object, gem, outdir, genome, pfm = NULL,
  species = c("auto", "human", "mouse"),
  condition_col = "condition", celltype_col = "cell_type",
  cell_type = NULL, rna_assay = "RNA", atac_assay = "ATAC",
  pando_args = list(), target_rsq_threshold = 0.05, workers = 10L,
  progress = getOption("RegCompassR.progress", TRUE))

rc_regcompass_step_metacells(object, outdir,
  condition_col = "condition", celltype_col = "cell_type",
  cell_type = NULL, rna_assay = "RNA", atac_assay = "ATAC",
  fragment_files = NULL, metacell_args = list(), workers = 10L,
  progress = getOption("RegCompassR.progress", TRUE), grn = NULL)

rc_regcompass_step_meta_modules(grn, metacells, gem, outdir,
  meta_module_args = list(),
  progress = getOption("RegCompassR.progress", TRUE))

rc_regcompass_step_layer1(grn, metacells, meta_modules, gem, outdir,
  gpr_and_method = c("min", "median", "mean"),
  gene_half_saturation = getOption("RegCompassR.cpm_half_saturation", 1),
  workers = 10L,
  progress = getOption("RegCompassR.progress", TRUE))

rc_regcompass_step_layer2(layer1, meta_modules, gem, medium_scenarios,
  outdir, model_mode = c("meta_module_gem", "full_gem"),
  layer2_args = list(), workers = 10L,
  progress = getOption("RegCompassR.progress", TRUE))

rc_regcompass_step_results(grn, metacells, meta_modules, layer1, layer2,
  gem, outdir, species = c("auto", "human", "mouse"),
  progress = getOption("RegCompassR.progress", TRUE))
}
\arguments{
\item{object}{Paired-cell Seurat RNA+ATAC object.}
\item{gem}{Prepared genome-scale metabolic model.}
\item{outdir}{Stage output directory.}
\item{genome}{Genome object matching ATAC coordinates and regulatory regions.}
\item{pfm}{Optional motif collection.}
\item{species}{\code{"auto"}, \code{"human"}, or \code{"mouse"}.}
\item{condition_col, celltype_col}{Condition and broad-cell-type metadata columns.}
\item{cell_type}{Optional broad-cell-type subset.}
\item{rna_assay, atac_assay}{RNA and ATAC assay names. In Stage 2, when \code{grn} is supplied and either assay argument is omitted, the corresponding assay name is inherited from Stage 1. Explicitly supplying a different assay remains a cross-stage contract error.}
\item{pando_args}{Stage 1 Pando configuration. \code{min_cells} defaults to \code{500L}. Multi-condition cell types use the fixed conditional E-star/JSE production route with \code{z=0.25}; current conditional controls in \code{pando_infer_args} are \code{tf_cor}, \code{peak_cor}, \code{padj_threshold}, \code{rank_action}, and \code{min_residual_df} (plus the canonical RNA/ATAC layer fields). Conditional ridge-CV, alternative-z and fusion-ratio controls are removed. One-condition cell types use standard Pando and may use its separate \code{ridge_control}.}
\item{target_rsq_threshold}{Standard-Pando full-data target R-squared gate, default \code{0.05}. The value is also recorded for conditional diagnostics, but conditional E-star/JSE exact-edge admission does not use R-squared as a hard gate.}
\item{workers}{Single worker cap used by computational stages, default \code{10L}. Stage 1 uses one parallel level at a time and reuses the worker budget for target-level Pando work.}
\item{progress}{Display and persist stage progress.}
\item{fragment_files}{Optional Stage 2 raw fragment input.}
\item{metacell_args}{Stage 2 reductions, dimensions, WNN and fragment controls. \code{gamma} controls the condition-specific initial hierarchy resolution; \code{min_metacell_size} is enforced by local same-condition repair of undersized fragments on the shared Walktrap hierarchy, and \code{min_metacells_per_stratum} remains a hard final-count constraint.}
\item{grn}{Stage 1 output. In Stage 2, supplying it enforces the retained Stage 1 cell set and supplies omitted Stage 1 assay names.}
\item{metacells, meta_modules, layer1, layer2}{Validated outputs from preceding stages.}
\item{meta_module_args}{Optional Stage 3 configuration, including a custom subsystem table.}
\item{gpr_and_method}{GPR AND aggregation method: \code{"min"}, \code{"median"}, or \code{"mean"}.}
\item{gene_half_saturation}{Half-saturation parameter for the bounded structural-support path.}
\item{medium_scenarios}{Shared medium table, normally created by \code{rc_make_medium_scenarios()}.}
\item{model_mode}{\code{"meta_module_gem"} or supplementary \code{"full_gem"}.}
\item{layer2_args}{Stage 5 controls. Supported top-level entries are \code{model_params}, \code{omega}, \code{target_direction}, \code{solver}, and \code{flux_threshold}. CORDA2 is the default completion under \code{"meta_module_gem"}; adjustable CORDA2 options are documented in \code{docs/layer2-corda.md}.}
}
\value{Each function returns a validated stage object and writes its stage checkpoint and diagnostics to \code{outdir}.}
\description{Runs individual stages of the RegCompass workflow for restartable execution. In the conditional Stage 1 route, Pando freezes the pooled/global-plus-condition exact-edge union dictionary, fits E-star at fixed \code{z=0.25}, obtains fusion-component joint covariance, applies BH within each condition-by-target family, and hands the same admitted exact-edge set to every condition whenever any condition is BH-supported and every condition coefficient is valid. Target R-squared remains diagnostic on this route. See \code{docs/tutorial-02-stepwise-audit.md} for current calls and \code{docs/mathematical-model.md} for quantitative definitions.}
