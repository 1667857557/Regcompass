% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stepwise_workflow.R
\name{rc_regcompass_step_metacells}
\alias{rc_regcompass_step_metacells}
\alias{rc_regcompass_step_meta_modules}
\alias{rc_regcompass_step_layer1}
\alias{rc_regcompass_step_layer2}
\alias{rc_regcompass_step_results}
\title{Run RegCompass through inspectable workflow stages}
\usage{
rc_regcompass_step_metacells(
  object,
  outdir,
  sample_col = "sample_id",
  condition_col = "condition",
  celltype_col = "cell_type",
  rna_assay = "RNA",
  atac_assay = "ATAC",
  fragment_files = FALSE,
  metacell_args = list()
)

rc_regcompass_step_meta_modules(
  metacells,
  gem,
  outdir,
  pfm,
  genome,
  pando_args = list(),
  layer1_args = list(),
  parallel = TRUE,
  BPPARAM = NULL
)

rc_regcompass_step_layer1(
  metacells,
  meta_modules,
  gem,
  outdir,
  regulatory_alpha = 1,
  tau = 0.2,
  gene_half_saturation = getOption("RegCompassR.cpm_half_saturation", 1),
  parallel = TRUE,
  BPPARAM = NULL
)

rc_regcompass_step_layer2(
  layer1,
  meta_modules,
  gem,
  medium_scenarios,
  outdir,
  model_mode = c("meta_module_gem", "full_gem"),
  layer2_args = list(),
  parallel = TRUE,
  BPPARAM = NULL
)

rc_regcompass_step_results(
  metacells,
  meta_modules,
  layer1,
  layer2,
  gem,
  outdir,
  species = c("auto", "human", "mouse")
)
}
\arguments{
\item{object}{A Seurat RNA+ATAC object.}

\item{outdir}{Output directory for the current stage or final result.}

\item{sample_col, condition_col, celltype_col}{Metadata columns identifying biological samples, conditions and cell types.}

\item{rna_assay, atac_assay}{RNA and ATAC assay names.}

\item{fragment_files}{Must be \code{FALSE} for the canonical condition-pooled workflow.}

\item{metacell_args}{Arguments passed to the SuperCell2 metacell builder. Tutorials use \code{gamma = 20}.}

\item{metacells}{Output from \code{rc_regcompass_step_metacells()}.}

\item{gem}{A prepared and validated genome-scale metabolic model.}

\item{pfm}{Motif position-frequency matrices.}

\item{genome}{Genome object matching the ATAC coordinates.}

\item{pando_args}{Arguments passed to condition-by-cell-type Pando inference. Tutorials use \code{peak_cor = 0}.}

\item{layer1_args}{Structural Layer 1 arguments used during local FASTCORE completion.}

\item{parallel}{Whether to parallelize condition-by-cell-type Pando fits, reaction-level GPR aggregation, or shared-model-by-metacell LP tasks in Steps 2, 3, and 4, respectively.}

\item{BPPARAM}{\code{NULL} to use the default RegCompass backend, \code{FALSE} for sequential execution, or a \code{BiocParallelParam} object. Logical \code{TRUE} is not valid.}

\item{meta_modules}{Output from \code{rc_regcompass_step_meta_modules()}.}

\item{regulatory_alpha}{Strength of the ATAC regulatory modifier on RNA support log-odds.}

\item{tau}{Boltzmann soft-min parameter for GPR AND rules.}

\item{gene_half_saturation}{Half-saturation used for absolute RNA support.}

\item{layer1}{Output from \code{rc_regcompass_step_layer1()}.}

\item{medium_scenarios}{One shared medium table used by every scored unit.}

\item{model_mode}{Either the shared union meta-module GEM or the shared full GEM.}

\item{layer2_args}{Additional non-reserved arguments passed to microCOMPASS scoring.}

\item{layer2}{Output from \code{rc_regcompass_step_layer2()}.}

\item{species}{Species identifier or \code{"auto"}.}
}
\value{
Each stage returns the complete object required by the next stage and writes an RDS checkpoint. The final stage returns the canonical RegCompass result list.
}
\description{
These functions expose the same canonical condition-pooled workflow as the convenience runner, but split execution into restartable stages. Users can inspect stage outputs, change parameters belonging to a later stage, and rerun only that stage and its descendants.
}
\details{
The stages are: condition-pooled metacell construction; Pando GRN and biological meta-module construction; RNA+ATAC Layer 1 reaction expression; directional COMPASS-like Layer 2 scoring; and final ranking/result assembly. Parameters should be changed only before the stage that consumes them. Changing an upstream stage invalidates every downstream checkpoint.

Peaks with zero counts are excluded before shared TF-IDF normalization and within each Pando group. In Layer 2, unmeasured reaction expression is zero-filled and receives the same strictest expression-linked penalty as explicitly measured zero expression.
}
\examples{
\dontrun{
bp <- BiocParallel::SnowParam(workers = 8, type = "SOCK")

step1 <- rc_regcompass_step_metacells(
  object = object,
  outdir = "steps/01_metacells",
  metacell_args = list(gamma = 20, BPPARAM = bp)
)

step2 <- rc_regcompass_step_meta_modules(
  metacells = step1,
  gem = gem,
  outdir = "steps/02_meta_modules",
  pfm = motifs,
  genome = genome,
  pando_args = list(
    pando_infer_args = list(
      method = "glm",
      tf_cor = 0.1,
      peak_cor = 0,
      adjust_method = "fdr"
    )
  ),
  parallel = TRUE,
  BPPARAM = bp
)

step3 <- rc_regcompass_step_layer1(
  metacells = step1,
  meta_modules = step2,
  gem = gem,
  outdir = "steps/03_layer1",
  parallel = TRUE,
  BPPARAM = bp
)

step4 <- rc_regcompass_step_layer2(
  layer1 = step3,
  meta_modules = step2,
  gem = gem,
  medium_scenarios = medium_scenarios,
  outdir = "steps/04_layer2",
  parallel = TRUE,
  BPPARAM = bp
)
}
}
