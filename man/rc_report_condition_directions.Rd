% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/condition_direction_report.R
\name{rc_report_condition_directions}
\alias{rc_report_condition_directions}
\title{Build a direction-aware condition report}
\usage{
rc_report_condition_directions(
  x,
  condition_col = NULL,
  celltype_col = NULL,
  conditions = NULL,
  cell_types = NULL,
  comparisons = NULL,
  reaction_ids = NULL,
  medium_scenarios = NULL,
  min_units = 5L,
  include_omnibus = TRUE,
  p_adjust_method = "BH",
  p_adjust_scope = c("celltype_contrast_medium", "celltype_contrast", "celltype",
    "global"),
  wilcox_correct = FALSE,
  eps = 1e-08,
  vmax_tolerance = 1e-06,
  direction_tolerance = 1e-10,
  include_unit_metrics = FALSE,
  source_label = NULL,
  outdir = NULL
)
}
\arguments{
\item{x}{A microCOMPASS result or RegCompass result containing one.}

\item{condition_col, celltype_col, conditions, cell_types, comparisons, reaction_ids, medium_scenarios, min_units, include_omnibus, p_adjust_method, p_adjust_scope, wilcox_correct, eps, vmax_tolerance}{Arguments passed to \code{rc_test_condition_reactions()}. Both allowed target directions are always requested.}

\item{direction_tolerance}{Non-negative tolerance used to classify paired forward and reverse unit scores as numerically indistinguishable.}

\item{include_unit_metrics}{Include the unit-level forward, reverse, any-direction, and balance table in the returned object.}

\item{source_label}{Optional provenance label, for example \code{"original_layer2_core"} or \code{"target_union_second_pass_noncore"}.}

\item{outdir}{Optional output directory.}
}
\value{
A \code{regcompass_condition_direction_report} list containing the original
\code{directional_pairwise} and \code{directional_omnibus} tables,
direction-collapsed \code{reaction_pairwise} and \code{reaction_omnibus}
tables, \code{direction_diagnostics}, the underlying
\code{directional_statistics}, and optionally \code{unit_metrics}.
}
\description{
Runs \code{rc_test_condition_reactions()} with both allowed LP directions and
preserves the COMPASS-style direction-specific results. It additionally derives
two non-additive reaction-level summaries: \code{any_direction_support} is the
larger available forward or reverse support score, and
\code{directional_balance} is forward support minus reverse support.
}
\details{
Forward and reverse scores remain independent counterfactual LP targets. They
are never added. \code{any_direction_support} reports the best-supported
available direction without double counting numerically identical directions.
\code{directional_balance} reports support asymmetry and must not be interpreted
as net flux. The direction diagnostic table explicitly identifies one-direction
targets, distinguishable bidirectional targets, and numerically indistinguishable
bidirectional targets.
}
\examples{
\dontrun{
report <- rc_report_condition_directions(
  result,
  condition_col = "dataset",
  celltype_col = "epithelial_or_stem",
  conditions = c("control_24hr", "JQ1_24hr", "MS177_24hr"),
  cell_types = c("epithelial_like", "stem-cell_like"),
  medium_scenarios = "high_glucose",
  source_label = "original_layer2_core",
  outdir = "RegCompass_result/07_direction_report"
)

report$directional_pairwise
report$reaction_pairwise
report$direction_diagnostics
}
}
