% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supercell2_current_contract.R
\name{rc_make_supercell2_metacells}
\alias{rc_make_supercell2_metacells}
\title{Build SuperCell2 metacells from explicit RegCompass strata}
\usage{
rc_make_supercell2_metacells(object, outdir,
  strata_cols = "condition", label_col = NULL,
  rna_assay = "RNA", atac_assay = "ATAC",
  rna_reduction = "pca", atac_reduction = "lsi",
  rna_dims = 1:30, atac_dims = 2:30,
  gamma = 30, seed = 12345L,
  min_cells_per_stratum = 100L, min_metacell_size = 20L,
  min_metacells_per_stratum = 2L,
  fragment_files = FALSE, bgzip_path = "bgzip", tabix_path = "tabix",
  fragment_nb_cl = 1L, save_metacell_object = TRUE,
  save_counts = TRUE, overwrite = FALSE, BPPARAM = NULL,
  on_stratum_error = c("record", "stop"), ...)
}
\arguments{
\item{object}{A preprocessed paired RNA+ATAC Seurat object.}
\item{outdir}{Directory for per-stratum metacell checkpoints.}
\item{strata_cols}{Complete metadata columns used by RegCompass to split cells before each SuperCell2 call. The canonical workflow uses the condition column.}
\item{label_col}{Optional complete categorical metadata column passed to \code{SuperCell::SCimplify_for_Seurat(label = ...)}. The canonical workflow uses the cell-type column.}
\item{rna_assay, atac_assay}{RNA and ATAC assay names.}
\item{rna_reduction, atac_reduction}{Precomputed reductions used for multimodal metacell construction.}
\item{rna_dims, atac_dims}{Dimensions used from the RNA and ATAC reductions.}
\item{gamma}{SuperCell graining level.}
\item{seed}{Integer random seed.}
\item{min_cells_per_stratum}{Minimum input cells required for a stratum.}
\item{min_metacell_size}{Metacells below this membership size are marked as low power.}
\item{min_metacells_per_stratum}{Minimum number of metacells required for a completed stratum.}
\item{fragment_files}{\code{FALSE} for aggregation of the existing ATAC peak-count assay, one fragment path, a list named by exact RegCompass stratum ID, or a manifest with \code{stratum_id} and \code{fragment_file}.}
\item{bgzip_path, tabix_path}{Executables used when SuperCell2 aggregates fragment files.}
\item{fragment_nb_cl}{Worker count passed to SuperCell2 fragment aggregation.}
\item{save_metacell_object}{Save each per-stratum Seurat metacell object.}
\item{save_counts}{Save per-stratum RNA and ATAC count matrices. This must be \code{TRUE}.}
\item{overwrite}{Rebuild existing complete stratum checkpoints.}
\item{BPPARAM}{Optional BiocParallel parameter object for the RegCompass stratum loop.}
\item{on_stratum_error}{Record failed strata or stop immediately.}
\item{...}{Additional formal arguments of the installed \code{SuperCell::SCimplify_for_Seurat()} function. Unknown arguments are rejected.}
}
\description{
Splits a Seurat object by explicit RegCompass strata, calls the current
SuperCell2 Seurat builder for each stratum, and imports aligned multimodal
metacell counts and membership metadata.
}
\details{
The active API has no \code{sample_col} or \code{pool_col} argument. In the
canonical RegCompass workflow, cells are split by condition and cell type is
supplied through SuperCell2's \code{label} argument. Membership is audited after
aggregation, and downstream Layer 1 requires label-pure metacells.

When \code{fragment_files = FALSE}, the existing ATAC peak-count assay is
aggregated. Fragment provenance, when requested, is keyed by exact RegCompass
stratum IDs rather than by a biological-sample column.
}
\value{
A list containing aligned metacell metadata, membership, RNA and ATAC count
matrices, optional fragment provenance, per-stratum status, and construction
contract fields.
}
