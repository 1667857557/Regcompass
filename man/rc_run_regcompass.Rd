% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regcompass.R and R/sample_bootstrap_timing_policy.R
\name{rc_run_regcompass}
\alias{rc_run_regcompass}
\title{Run the shared-background condition-sub-GRN RegCompass workflow}
\usage{
rc_run_regcompass(object, gem, outdir, genome, pfm = NULL,
  species = c("auto", "human", "mouse"),
  condition_col = "condition", celltype_col = "cell_type",
  sample_col = NULL, rna_assay = "RNA", atac_assay = "ATAC",
  grn_mode = c("multitask_shared_backbone", "legacy_condition_pando"),
  pando_args = list(), multitask_args = list(),
  fragment_files = FALSE, metacell_args = list(),
  meta_module_args = list(), layer1_args = list(),
  medium_scenarios = NULL,
  model_mode = c("meta_module_gem", "full_gem"), layer2_args = list(),
  upstream_workers = 6L, layer2_workers = 30L,
  progress = getOption("RegCompassR.progress", TRUE))
}
\arguments{
\item{object}{Paired single-cell RNA+ATAC Seurat object.}
\item{gem}{Prepared genome-scale metabolic model.}
\item{outdir}{Persistent workflow output directory.}
\item{genome}{Genome object matching the ATAC coordinates and motif collection.}
\item{pfm}{Optional motif position-frequency matrices.}
\item{species}{Model species or \code{"auto"} inference from GEM provenance.}
\item{condition_col, celltype_col}{Complete condition and cell-type metadata columns.}
\item{sample_col}{Optional Stage 1 donor/sample metadata column. When valid, bootstrap replicates resample sample clusters with replacement separately inside each condition and retain all cells from each selected sample. When \code{NULL}, or when the named column does not exist, RegCompass prints the exact fallback reason and uses condition-stratified cell resampling. An existing column with missing or empty identifiers is an error. This argument does not change Stage 2 metacell strata.}
\item{rna_assay, atac_assay}{RNA and ATAC assay names.}
\item{grn_mode}{Canonical shared-background direct-theta GRN or legacy independent Pando fits.}
\item{pando_args}{Stage 1 Pando candidate-design controls. A legacy embedded \code{sample_col} is accepted only when it agrees with the top-level value.}
\item{multitask_args}{Elastic-net, cross-validation, bootstrap, observability, and active-edge controls.}
\item{fragment_files}{Use \code{FALSE} for the canonical existing-peak-count path.}
\item{metacell_args}{Stage 2 condition-only SuperCell2 controls.}
\item{meta_module_args}{Optional Stage 3 custom \code{subsystem_table}.}
\item{layer1_args}{Stage 4 integrated-evidence controls.}
\item{medium_scenarios}{Condition-invariant medium table used by Stage 5.}
\item{model_mode}{Medium-specific union GEM or complete GEM.}
\item{layer2_args}{Layer 2 scoring and union-GEM controls.}
\item{upstream_workers}{Worker count for GRN inference and Layer 1.}
\item{layer2_workers}{Worker count for Stage 5 union-GEM completion and scoring.}
\item{progress}{Display workflow and stage progress.}
}
\description{
Runs the six shared-background GRN, condition-only metacell, condition meta-module, Layer 1, medium-specific union-GEM/Layer 2, and result stages.
}
\details{
With a valid \code{sample_col}, Stage 1 uses a condition-stratified nonparametric cluster bootstrap. For each condition, the observed number of sample IDs is drawn with replacement; each draw contributes all cells belonging to that sample and condition. Cluster sizes are preserved, so bootstrap cell counts may vary. Sample identifiers may be shared across conditions in paired designs because resampling is performed independently within condition.

If \code{sample_col} is omitted or absent, a warning is printed and the previous condition-stratified cell bootstrap is used. Bootstrap provenance is retained in Stage 1 coefficient, stability, cell-type status, and group-status outputs.

Stage 2 remains condition-only. Stage 3 uses the stability-selected condition target genes to define complete-GPR cores. Stage 5 merges the condition catalogues and reuses one identical medium-specific union GEM across all conditions and metacells.

Elapsed time and final status are printed in the R console for each stage. Timing is not stored in result objects and no \code{step_timing.tsv} or \code{00_execution_timing.tsv} file is written.
}
