% Generated by roxygen2: do not edit by hand
\name{regcompass_steps}
\alias{rc_regcompass_step_grn}
\alias{rc_regcompass_step_metacells}
\alias{rc_regcompass_step_meta_modules}
\alias{rc_regcompass_step_layer1}
\alias{rc_regcompass_step_layer2}
\alias{rc_regcompass_step_results}
\title{Run the restartable RegCompass stages}
\usage{
rc_regcompass_step_grn(object, gem, outdir, genome, pfm = NULL,
  species = c("auto", "human", "mouse"),
  condition_col = "condition", celltype_col = "cell_type",
  cell_type = NULL, rna_assay = "RNA", atac_assay = "ATAC",
  pando_args = list(), target_rsq_threshold = 0.05, workers = 10L,
  progress = getOption("RegCompassR.progress", TRUE))

rc_regcompass_step_metacells(object, outdir,
  condition_col = "condition", celltype_col = "cell_type",
  cell_type = NULL, rna_assay = "RNA", atac_assay = "ATAC",
  fragment_files = NULL, metacell_args = list(), workers = 10L,
  progress = getOption("RegCompassR.progress", TRUE), grn = NULL)

rc_regcompass_step_meta_modules(grn, metacells, gem, outdir,
  meta_module_args = list(),
  progress = getOption("RegCompassR.progress", TRUE))

rc_regcompass_step_layer1(grn, metacells, meta_modules, gem, outdir,
  gpr_and_method = c("min", "median", "mean"),
  gene_half_saturation = getOption("RegCompassR.cpm_half_saturation", 1),
  workers = 10L,
  progress = getOption("RegCompassR.progress", TRUE))

rc_regcompass_step_layer2(layer1, meta_modules, gem, medium_scenarios,
  outdir, model_mode = c("meta_module_gem", "full_gem"),
  layer2_args = list(), workers = 10L,
  progress = getOption("RegCompassR.progress", TRUE))

rc_regcompass_step_results(grn, metacells, meta_modules, layer1, layer2,
  gem, outdir, species = c("auto", "human", "mouse"),
  progress = getOption("RegCompassR.progress", TRUE))
}
\arguments{
\item{object}{Paired-cell Seurat RNA+ATAC object.}
\item{gem}{Prepared genome-scale metabolic model.}
\item{outdir}{Stage output directory.}
\item{genome}{Genome object matching ATAC coordinates and regulatory regions.}
\item{pfm}{Optional motif collection.}
\item{species}{\code{"auto"}, \code{"human"}, or \code{"mouse"}.}
\item{condition_col, celltype_col}{Condition and broad-cell-type metadata columns.}
\item{cell_type}{Optional broad-cell-type subset.}
\item{rna_assay, atac_assay}{RNA and ATAC assay names.}
\item{pando_args}{Stage 1 Pando controls. \code{min_cells} defaults to \code{500L}. Multi-condition broad cell types use the common-dictionary no-fusion ridge route; one-condition broad cell types use the K=1 standard ridge route unless another standard method is explicitly selected.}
\item{target_rsq_threshold}{RegCompass target-model quality threshold applied to the selected-lambda final full-data Pando R-squared after Pando edge significance. Default \code{0.05}. OOF R-squared remains diagnostic only.}
\item{workers}{Workflow-level process cap used by computational stages, default \code{10L}.}
\item{progress}{Display and persist stage progress.}
\item{fragment_files}{Optional Stage 2 raw ATAC fragment input. Membership is finalized before fragment aggregation/recount.}
\item{metacell_args}{Stage 2 geometry and repair controls. Defaults include RNA PCA 1:30, ATAC LSI 2:30, \code{gamma = 30L}, \code{k.knn = 30L}, \code{min_cells_per_stratum = 20L}, \code{min_metacell_size = 1L}, and \code{min_metacells_per_stratum = 1L}. If \code{min_metacell_size > 1L}, an explicit \code{min_merge_affinity} is required. Repair is restricted to the same condition and broad cell type using the exact original shared WNN; \code{unresolved_small_policy} is \code{"error"} or \code{"keep"}.}
\item{grn}{Stage 1 output. In Stage 2, supplying it enforces the exact retained Stage 1 cell set and metadata design.}
\item{metacells, meta_modules, layer1, layer2}{Validated outputs from preceding stages.}
\item{meta_module_args}{Optional Stage 3 configuration, including a custom subsystem table.}
\item{gpr_and_method}{GPR AND aggregation method: \code{"min"}, \code{"median"}, or \code{"mean"}.}
\item{gene_half_saturation}{Half-saturation parameter for bounded structural support.}
\item{medium_scenarios}{Medium table, normally created by \code{rc_make_medium_scenarios()}. Built-in concentration challenge metadata are not automatically converted to uptake flux bounds.}
\item{model_mode}{\code{"meta_module_gem"} or supplementary \code{"full_gem"}.}
\item{layer2_args}{Stage 5 controls. Supported top-level entries are \code{model_params}, \code{omega}, \code{target_direction}, \code{solver}, and \code{flux_threshold}. CORDA2 is the default context-specific completion under \code{"meta_module_gem"}.}
}
\value{Each function returns a validated stage object and writes its stage checkpoint and diagnostics to \code{outdir}. The Stage 2 object contains the final repaired membership; downstream stages use that membership only.}
\description{Runs the current restartable RegCompass stages. Broad-cell-type metacells use one shared multimodal WNN across conditions, condition splitting occurs after Walktrap clustering, and optional small-metacell repair uses that exact original WNN.}
\seealso{\code{rc_run_regcompass()}, \code{rc_run_regcompass_one_shot()}, \code{rc_make_medium_scenarios()}}
