% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/target_union.R and R/target_union_anchors.R
\name{rc_regcompass_step_target_union}
\alias{rc_regcompass_step_target_union}
\title{Score database-linked non-core reactions in final union GEMs}
\usage{
rc_regcompass_step_target_union(
  layer1, meta_modules, layer2, gem, outdir,
  core_reaction_ids = NULL, core_genes = NULL,
  gene_match = c("complete_gpr", "any_direct"),
  layer2_args = list(), parallel = TRUE, BPPARAM = NULL,
  progress = getOption("RegCompassR.progress", TRUE)
)
}
\arguments{
\item{layer1, meta_modules, layer2}{Validated outputs from the original \code{meta_module_gem} run. Stage 3 supplies the merged biological catalogue; Stage 5 supplies the final cached medium-specific union GEMs.}
\item{gem}{The same GEM used for the original run.}
\item{outdir}{Output directory.}
\item{core_reaction_ids}{GEM reaction IDs used only as direct database-mapping anchors. The argument name is retained for compatibility, but the selected reactions may be original Layer 2 cores or non-core GEM reactions.}
\item{core_genes}{Genes used to resolve original core anchors through GPR rules. Every supplied gene selector must resolve to at least one originally scored core.}
\item{gene_match}{Complete-GPR or intentional direct-gene matching for \code{core_genes}.}
\item{layer2_args}{Optional \code{omega}, \code{target_direction}, \code{solver}, and \code{flux_threshold} overrides. This step does not rebuild a union GEM or rerun FASTCORE.}
\item{parallel}{Whether to parallelize reused-model by metacell tasks.}
\item{BPPARAM}{Optional BiocParallel parameter object.}
\item{progress}{Display stage progress.}
}
\value{A \code{regcompass_target_union_step} containing selected anchors, original-core and non-core anchor subsets, direct database-linked non-core targets, source-model provenance, and LP results.}
\description{Directly maps selected GEM reaction anchors through shared KEGG, Reactome, or master-Rhea identifiers and scores linked non-core reactions in the exact final Stage 5 union GEMs.}
\details{Reaction-ID anchors are validated against the supplied GEM rather than restricted to the original Layer 2 core set. Gene-selected anchors retain complete-GPR core resolution. Same-subsystem, metabolite-neighbour, recursive/transitive, model-reconstruction, and new FASTCORE expansion are excluded. The exact cached medium-specific union GEM files and bounds are reused. Each cache file is validated against its recorded checksum, build strategy, completion stage, union-GEM marker, and medium identifier. Candidate availability is checked against the actual reactions present in every required cached model, not only the Stage 3 catalogue. Original Layer 2 core targets are not rescored. Elapsed time and final status are printed in R and are not stored in the returned object or a timing TSV file.}
