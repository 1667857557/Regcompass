% Generated by roxygen2: do not edit by hand
\name{rc_make_medium_scenarios}
\alias{rc_make_medium_scenarios}
\title{Build biological extracellular medium scenarios}
\usage{
rc_make_medium_scenarios(
  gem,
  scenario = "normal_human_plasma",
  species = c("auto", "human", "mouse"),
  custom_medium = NULL,
  custom_metabolites = NULL,
  uptake_scale = 1,
  exchange_roles = c("exchange"),
  condition = "all",
  exchange_limit = 1,
  strict_preset_matching = TRUE
)
}
\arguments{
\item{gem}{A validated RegCompass GEM.}
\item{scenario}{One or more built-in identifiers: \code{"normal_human_plasma"}, \code{"mouse_plasma"}, \code{"high_glucose"}, \code{"low_glucose"}, \code{"high_lactate"}, \code{"low_lactate"}, \code{"low_glutamine"}, or \code{"custom"}. Use \code{NULL} for custom-only input.}
\item{species}{\code{"auto"}, \code{"human"}, or \code{"mouse"}. Human plasma/challenge scenarios require Human-GEM and mouse plasma requires Mouse-GEM.}
\item{custom_medium}{Optional reaction-level data frame with \code{medium_scenario_id}, \code{exchange_reaction_id}, \code{lb}, \code{ub}, and \code{available}. These are explicit user-supplied flux-bound assumptions.}
\item{custom_metabolites}{Optional metabolite-level data frame with \code{metabolite_name} and optional availability, concentration, explicit \code{uptake_fraction}, matching, and provenance fields.}
\item{uptake_scale}{Non-negative global or named multiplier applied only to an explicit relative uptake assumption. Built-in challenge concentrations are not automatically converted into uptake fluxes.}
\item{exchange_roles}{Reaction roles treated as exchange reactions.}
\item{condition}{Shared condition label; canonical scoring normally uses \code{"all"}.}
\item{exchange_limit}{Shared model flux cap intersected with original GEM directionality.}
\item{strict_preset_matching}{Stop when required components cannot be mapped to GEM exchanges.}
}
\value{A reaction-level medium table with exchange bounds and provenance fields.}
\description{Builds predefined biological media or explicit user-defined extracellular constraints. Built-in challenge concentrations are retained as biological metadata; concentration in mM is not treated as an uptake flux without an explicit flux assumption.}
\details{
Every final bound is intersected with the original GEM directionality during medium application. The full-GEM cache identity is based on the base GEM plus the resolved final reaction bounds, so distinct scenario descriptions that produce identical final bounds share the same feasible-region fingerprint.
}
